/**
     * Get a default {{ propertyInDescription|trim }} value, if any is available
     *
     * @return {{- dataType == 'mixed' ? " mixed " : " #{dataType}|null " -}} Default {{ propertyInDescription|trim }} value or null if no default value is available
     */
