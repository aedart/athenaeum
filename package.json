{
    "devDependencies": {
        "@vuepress/plugin-back-to-top": "^1.3.1",
        "cross-env": "^7.0.0",
        "vue-tabs-component": "^1.5.0",
        "vuepress": "^1.8.2",
        "vuepress-plugin-element-tabs": "^0.2.8"
    },
    "scripts": {
        "docs:dev": "cross-env NODE_ENV=development NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vuepress dev docs",
        "docs:build": "cross-env NODE_ENV=development NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vuepress build docs",
        "docs:build-prod": "cross-env NODE_ENV=production NODE_OPTIONS=--openssl-legacy-provider ./node_modules/.bin/vuepress build docs"
    }
}
